name: NodeJS with Gulp

on:
  push:
    branches: [ main, new-sequence-load1 ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [14.x]
    
    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        yarn install
        ls
        gulp
    - name: Install SSH Key
      uses: shimataro/ssh-key-action@v2
      with:
        key: {%- raw -%} ${{ secrets.SSH_PRIVATE_KEY }}
        known_hosts: 'just-a-placeholder-so-we-dont-get-errors'
    - name: Deploy with rsync
      run: rsync -avz ./dist/ smartora@smartora.ftp.tools:/smarto.agency/main/
    # - name: 📂 Sync files
    #   uses: SamKirkland/web-deploy@v1
    #   with:
    #     target-server: smartora.ftp.tools
    #     remote-user: smartora
    #     ssh-port: 22
    #     private-ssh-key: ${{ secrets.SSH_PRIVATE_KEY }}
    #     source-path: ./dist/
    #     destination-path: ~/smarto.agency/main/
    #     rsync-options: --size-only --human-readable	 --checksum
